---
import galleryData from "../../data/gallery.json";
import "../../styles/global.css";
import { Sun, Moon, Menu, X } from "lucide-astro";
const cities = galleryData.cities;
---

<html lang="en" class="scroll-smooth">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Out of Office — Gallery | Milad Natanzi</title>
        <meta
            name="description"
            content="A visual journal of places visited by Milad Natanzi. Stepping away from the servers and lab to capture the world in focus."
        />

        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            rel="preload"
            as="style"
            href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Newsreader:ital,opsz,wght@0,6..72,300;0,6..72,400;1,6..72,300;1,6..72,400&display=swap"
        />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Newsreader:ital,opsz,wght@0,6..72,300;0,6..72,400;1,6..72,300;1,6..72,400&display=swap"
            rel="stylesheet"
        />

        <script is:inline>
            if (
                localStorage.getItem("theme") === "dark" ||
                (!("theme" in localStorage) &&
                    window.matchMedia("(prefers-color-scheme: dark)").matches)
            ) {
                document.documentElement.classList.add("dark");
            } else {
                document.documentElement.classList.remove("dark");
            }
        </script>

        <style>
            body {
                font-family: "Inter", sans-serif;
            }
            .font-serif {
                font-family: "Newsreader", serif;
            }

            .glass-nav {
                background: rgba(250, 250, 250, 0.85);
                backdrop-filter: blur(16px);
                -webkit-backdrop-filter: blur(16px);
                border-bottom: 1px solid rgba(0, 0, 0, 0.04);
            }
            .dark .glass-nav {
                background: rgba(9, 9, 11, 0.85);
                border-bottom: 1px solid rgba(255, 255, 255, 0.04);
            }

            .city-card {
                position: relative;
                overflow: hidden;
                border-radius: 1rem;
                aspect-ratio: 4/5;
                cursor: pointer;
                display: block;
                text-decoration: none;
            }
            .city-card img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                transition: transform 0.8s ease, filter 0.5s ease;
                filter: grayscale(30%);
            }
            .city-card:hover img {
                transform: scale(1.06);
                filter: grayscale(0%);
            }
            .city-card-overlay {
                position: absolute;
                inset: 0;
                background: linear-gradient(
                    to top,
                    rgba(0, 0, 0, 0.75) 0%,
                    rgba(0, 0, 0, 0.1) 50%,
                    transparent 100%
                );
                transition: opacity 0.4s ease;
            }
            .city-card:hover .city-card-overlay {
                opacity: 0.85;
            }
            .city-card-text {
                position: absolute;
                bottom: 0;
                left: 0;
                right: 0;
                padding: 1.5rem;
            }
        </style>
    </head>

    <body
        class="bg-zinc-50 dark:bg-zinc-950 text-zinc-800 dark:text-zinc-200 antialiased min-h-screen transition-colors duration-300"
    >
        <!-- Navigation -->
        <nav
            class="fixed top-0 left-0 right-0 z-50 glass-nav transition-all duration-300"
            id="main-nav"
        >
            <div
                class="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between"
            >
                <a
                    href="/"
                    class="font-sans text-[1.35rem] font-bold tracking-tight text-transparent bg-clip-text bg-gradient-to-r from-zinc-900 to-zinc-500 dark:from-zinc-100 dark:to-zinc-400 hover:opacity-80 transition-opacity"
                >
                    Milad Natanzi
                </a>

                <div
                    class="hidden md:flex items-center space-x-8 text-[13px] font-montserrat font-normal tracking-[0.5px] text-zinc-500 dark:text-zinc-400 capitalize"
                >
                    <a
                        href="/#about"
                        class="nav-link py-1 hover:text-zinc-900 dark:hover:text-white transition-colors"
                        >About</a
                    >
                    <a
                        href="/#news"
                        class="nav-link py-1 hover:text-zinc-900 dark:hover:text-white transition-colors"
                        >News</a
                    >
                    <a
                        href="/cv"
                        class="nav-link py-1 hover:text-zinc-900 dark:hover:text-white transition-colors"
                        >CV</a
                    >
                    <a
                        href="/#experience"
                        class="nav-link py-1 hover:text-zinc-900 dark:hover:text-white transition-colors"
                        >Experience</a
                    >
                    <a
                        href="/#projects"
                        class="nav-link py-1 hover:text-zinc-900 dark:hover:text-white transition-colors"
                        >Projects</a
                    >
                    <a
                        href="/#blog"
                        class="nav-link py-1 hover:text-zinc-900 dark:hover:text-white transition-colors"
                        >Blog</a
                    >
                    <a
                        href="/gallery"
                        class="nav-link py-1 text-zinc-900 dark:text-white transition-colors font-medium"
                        >Out of office</a
                    >
                </div>

                <div class="flex items-center space-x-4">
                    <button
                        id="theme-toggle"
                        class="p-2 text-zinc-500 hover:text-zinc-900 dark:text-zinc-400 dark:hover:text-white transition-colors focus:outline-none rounded-full border border-transparent hover:border-zinc-200 dark:hover:border-zinc-800 hidden md:block"
                    >
                        <Sun class="hidden dark:block w-4 h-4" />
                        <Moon class="block dark:hidden w-4 h-4" />
                    </button>
                    <a
                        href="mailto:milad@natanzi.com"
                        class="hidden md:inline-flex items-center justify-center px-5 py-2 text-[13px] font-montserrat font-medium tracking-[0.5px] text-white bg-zinc-900 dark:bg-white dark:text-zinc-900 rounded-full hover:bg-zinc-800 dark:hover:bg-zinc-100 transition-colors shadow-sm"
                    >
                        Let's Talk
                    </a>
                </div>

                <!-- Mobile Menu Toggle -->
                <button
                    id="mobile-menu-btn"
                    class="md:hidden text-zinc-500 hover:text-zinc-900 p-2 -mr-2 focus:outline-none"
                >
                    <div id="icon-menu-wrapper"><Menu class="w-6 h-6" /></div>
                    <div id="icon-close-wrapper" class="hidden">
                        <X class="w-6 h-6" />
                    </div>
                </button>
            </div>
        </nav>

        <!-- Mobile Menu Overlay -->
        <div
            id="mobile-menu"
            class="fixed inset-0 bg-white/98 dark:bg-zinc-950/98 backdrop-blur-xl z-[60] hidden flex-col justify-center items-center opacity-0 transition-opacity duration-300"
        >
            <nav class="flex flex-col items-center space-y-6 w-full px-6">
                <a
                    href="/#about"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-800 dark:text-zinc-200 hover:text-blue-500 dark:hover:text-blue-400 transition-colors capitalize"
                    >About</a
                >
                <a
                    href="/#news"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-800 dark:text-zinc-200 hover:text-blue-500 dark:hover:text-blue-400 transition-colors capitalize"
                    >News</a
                >
                <a
                    href="/cv"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-800 dark:text-zinc-200 hover:text-blue-500 dark:hover:text-blue-400 transition-colors capitalize"
                    >CV</a
                >
                <a
                    href="/#experience"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-800 dark:text-zinc-200 hover:text-blue-500 dark:hover:text-blue-400 transition-colors capitalize"
                    >Experience</a
                >
                <a
                    href="/#projects"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-800 dark:text-zinc-200 hover:text-blue-500 dark:hover:text-blue-400 transition-colors capitalize"
                    >Projects</a
                >
                <a
                    href="/#blog"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-800 dark:text-zinc-200 hover:text-blue-500 dark:hover:text-blue-400 transition-colors capitalize"
                    >Blog</a
                >
                <a
                    href="/gallery"
                    class="mobile-link text-3xl font-montserrat font-light text-zinc-900 dark:text-white transition-colors capitalize"
                    >Out of office</a
                >

                <div class="pt-8 w-full max-w-[240px]">
                    <a
                        href="mailto:milad@natanzi.com"
                        class="mobile-link flex items-center justify-center px-6 py-4 text-[15px] font-montserrat font-medium tracking-[1px] text-white bg-zinc-900 dark:bg-white dark:text-zinc-900 rounded-full shadow-lg active:scale-95 transition-transform uppercase"
                    >
                        Let's Talk
                    </a>
                </div>
            </nav>
        </div>

        <main class="pt-32 pb-24 max-w-6xl mx-auto px-6">
            <!-- Header -->
            <header class="mb-16">
                <div class="flex items-center gap-3 mb-6">
                    <svg
                        class="w-5 h-5 text-zinc-400"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                        ><path
                            d="M23 19a2 2 0 01-2 2H3a2 2 0 01-2-2V8a2 2 0 012-2h4l2-3h6l2 3h4a2 2 0 012 2z"
                        ></path><circle cx="12" cy="13" r="4"></circle>
                    </svg>
                    <span
                        class="text-xs font-semibold uppercase tracking-widest text-zinc-400 dark:text-zinc-500"
                        >Out of Office</span
                    >
                </div>

                <p
                    class="text-lg text-zinc-500 dark:text-zinc-400 font-light max-w-xl"
                >
                    Stepping away from the servers and lab to capture the world
                    in focus.
                </p>
                <div class="mt-8 h-px bg-zinc-200 dark:bg-zinc-800"></div>
            </header>

            <!-- City Grid -->
            <div
                class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 lg:gap-6"
            >
                {
                    cities.map((city, i) => (
                        <a
                            href={`/gallery/${city.slug}`}
                            class="city-card group"
                        >
                            <img
                                src={city.cover}
                                alt={city.name}
                                loading={i === 0 ? "eager" : "lazy"}
                            />
                            <div class="city-card-overlay" />
                            <div class="city-card-text">
                                <p class="text-white/60 text-xs font-semibold uppercase tracking-widest mb-1">
                                    {city.photos.length} Photos
                                </p>
                                <h2 class="text-2xl font-serif text-white">
                                    {city.name}
                                </h2>
                                <p class="text-white/70 text-sm mt-1 font-light">
                                    {city.tagline}
                                </p>
                            </div>
                        </a>
                    ))
                }
            </div>

            <!-- Footer note -->
            <p
                class="mt-16 text-center text-sm text-zinc-400 dark:text-zinc-600 font-light italic"
            >
                More cities coming soon...
            </p>
        </main>

        <script>
            // Mobile Menu Toggle Logic
            const mobileBtn = document.getElementById("mobile-menu-btn");
            const mobileMenu = document.getElementById("mobile-menu");
            const iconMenuWrapper =
                document.getElementById("icon-menu-wrapper");
            const iconCloseWrapper =
                document.getElementById("icon-close-wrapper");
            const mobileLinks = document.querySelectorAll(".mobile-link");

            let menuOpen = false;

            document
                .getElementById("theme-toggle")
                ?.addEventListener("click", () => {
                    if (document.documentElement.classList.contains("dark")) {
                        document.documentElement.classList.remove("dark");
                        localStorage.setItem("theme", "light");
                    } else {
                        document.documentElement.classList.add("dark");
                        localStorage.setItem("theme", "dark");
                    }
                });

            function toggleMenu() {
                if (!mobileBtn) return;
                menuOpen = !menuOpen;
                if (menuOpen) {
                    mobileMenu.classList.remove("hidden");
                    setTimeout(() => {
                        mobileMenu.classList.remove("opacity-0");
                        mobileMenu.classList.add("opacity-100");
                        document.body.classList.add("no-scroll");
                        iconMenuWrapper.classList.add("hidden");
                        iconCloseWrapper.classList.remove("hidden");
                    }, 10);
                } else {
                    mobileMenu.classList.remove("opacity-100");
                    mobileMenu.classList.add("opacity-0");
                    document.body.classList.remove("no-scroll");
                    iconCloseWrapper.classList.add("hidden");
                    iconMenuWrapper.classList.remove("hidden");

                    setTimeout(() => {
                        mobileMenu.classList.add("hidden");
                    }, 300);
                }
            }

            if (mobileBtn) {
                mobileBtn.addEventListener("click", toggleMenu);
            }

            mobileLinks.forEach((link) => {
                link.addEventListener("click", () => {
                    if (menuOpen) toggleMenu();
                });
            });
        </script>
    </body>
</html>
